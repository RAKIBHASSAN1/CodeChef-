
#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,i,k1=0,k;
   cin>>n>>k;
   int a[n];
   for(i=0;i<n;i++)
   {
       cin>>a[i];
   }
   for(i=0;i<n;i++)
   {
      if(a[i]%k==0)
      {
          k1++;
      }
   }
   cout<<k1<<endl;

    return 0;
}
