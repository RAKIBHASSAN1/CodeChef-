#include<bits/stdc++.h>
using namespace std;
int main()
{
   long n,t,temp, d, sum = 0;
  cin>>t;
  while(t--)
  {
      sum=0;
    cin>>n;
    temp =n;
    while (n > 0)
    {
        d = n % 10;
        sum  = sum + d;
        n=n/10;
    }
    cout<<sum<<endl;
  }

}
