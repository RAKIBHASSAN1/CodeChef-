#include<bits/stdc++.h>
using namespace std;

int main(){
	int t,b,ls;
	float max,min;
	cin>>t;
	while(t--)
	{
		cin>>b>>ls;
		max=sqrt(b*b+ls*ls);
		 min=sqrt(ls*ls-b*b);
		cout<<min<<" "<<max<<endl;
		
		
	}	
	return 0;
}
